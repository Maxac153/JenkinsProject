#!groovy

pipeline {
    agent any
    triggers { pollSCM('* * * * *') }
    stages {
        stage('Image creation') {
            steps {
                sh '''
                    pyinstaller -w -F main.py
                '''
            }
        }
    }
}

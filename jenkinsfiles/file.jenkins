#!groovy

pipeline {
    agent any
    triggers { pollSCM('*****') }
    stages {
        stage('Image creation') {
            steps {
                sh '''
                    ls -a
                    pyinstaller -w -F main.py
                    chmod 777 ./dist/main
                '''
            }
        }
    }
}

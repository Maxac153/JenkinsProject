#!groovy

pipeline {
    agent any
    triggers { pollSCM('* * * * *') }
    stages {
        stage('Testing the project') {
            steps {
                sh '''
                    python3 -m pytest
                '''
            }
        }
        stage('Image creation') {
            steps {
                sh '''
                    pyinstaller -w -F main.py
                '''
            }
        }
    }
}
